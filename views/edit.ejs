<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/head.css">
</head>
<body>
    <%- include('partials/head.ejs') %>
    <div class='container'>
        <form action='/catalog' method='POST' enctype="multipart/form-data">
            <div class="mb-1">
              <img src="<%= catalog.image %>" width="300px">
              <div class="col-md-10">
              <label class="form-label">ชื่อสินค้า</label>
              <input type="text" class="form-control" name='catalog[name]' placeholder='<%= catalog.name %>' value="<%= catalog.name %>">
             </div>
            </div>
            <div class="mb-1">
              <div class="col-md-10">
                <label  class="form-label">รูปภาพ</label>
                <input type='file' class="form-control" name='image' accept="image/*"  placeholder='<%= catalog.image%>' value="<%= catalog.image %>"> 
              </div>
            </div>
            <div class="mb-1">
            <div class="row">
              <div class="col-sm-5">
                  <label class="form-label">ผู้แต่ง</label>
                  <input type='text'class="form-control"  name='catalog[author]' placeholder='<%= catalog.author %>' value="<%= catalog.author%>">
              </div>  

              <div class="col-sm-5">
                  <label class="form-label">สำนักพิมพ์</label>
                  <input type='text' class="form-control" name='catalog[publishing]' placeholder='<%= catalog.publishing %>' value="<%= catalog.publishing%>">
              </div>              
          </div>
          <div class="row">
            <div class="col-sm-5">
                <label class="form-label">จำนวนหน้า</label>
                <input type='text'class="form-control"  name='catalog[num_pages]' placeholder='<%= catalog.num_pages %>' value="<%= catalog.num_pages %>" >
            </div>  

            <div class="col-sm-5">
                <label class="form-label">ISBN</label>
                <input type='text' class="form-control" name='catalog[ISBN]' placeholder='<%= catalog.ISBN%>' value="<%= catalog.ISBN %>">
            </div>              
        </div>
        </div>
        <div class="mb-1">
            <div class="row">
              <div class="col-sm-5">
                  <label class="form-label">ราคา</label>
                  <input type='text'class="form-control"  name='catalog[price]' placeholder='<%= catalog.price%>' value="<%= catalog.price%>" >
              </div>  

              <div class="col-sm-5">
                  <label class="form-label">จำนวนเล่ม</label>
                  <input type='text' class="form-control" name='catalog[number]' id="number" placeholder='<%= catalog.number %>' value="<%= catalog.number %>" >
              </div>              
          </div>
        </div>
            
            <div class="mb-1">
              <div class="col-md-10">
            <label  class="form-label">โปรดเลือกประเภทของหนังสือ</label>
            <select class="form-select" aria-label="Default select example" name='catalog[type]' placeholder='<%= catalog.type %>' value="<%= catalog.type %>">
                <option value="comedy">นิยายตลกขบขัน</option>
                <option value="action">นิยายต่อสู้ </option>
                <option value="romance">นิยายรักโรแมนติก</option>
                <option value="boylove">นิยายวาย</option>
                <option value="scifi">นิยายวิทยาศาสตร์</option>
                <option value="fantasy">นิยายแฟนตาซี</option>
                <option value="thriller">นิยายสยองขวัญ</option>
                <option value="suspense">นิยายสืบสวนสอบสวน</option>
                <option value="historical">นิยายอิงประวัติศาสตร์</option>
              </select>
            </div>
            </div>
            <div class="mb-1">
              <div class="form-group">
                <label for="exampleFormControlTextarea1">คำโปรย</label>
                <textarea class="form-control" id="exampleFormControlTextarea1" rows="4" name='catalog[desc]' placeholder='<%= catalog.desc %>' value="<%= catalog.desc %>"></textarea>
              </div>
            <div class="mb-3">
          </div>
            <button type="submit" class="btn btn-primary">Submit</button>
          </form>
        </div>
      </div>
    <%- include('partials/footer.ejs') %>

</body>
</html>